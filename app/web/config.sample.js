// Copy this file to config.js and fill in your Naver Maps client key.
// The resulting config.js must be excluded from version control.
window.NAVER_MAP_KEY = '';

// Optional: provide the same admin token configured on the API server
// to enable admin-only controls (e.g., 지도에서 마커 삭제).
window.ADMIN_ACCESS_TOKEN = '';

// Optional: override the API endpoint. If omitted the app falls back to
// http://localhost:3000/api/v1 when running locally.
window.API_BASE_URL = '';
