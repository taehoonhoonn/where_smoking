# 🔗 카카오 API 키 공유 설정

이제 카카오 API 키가 다음 컴포넌트 간에 공유됩니다:

## 📍 공유 구성

### 1. API 서버 (Node.js)
- **파일**: `api_server/.env`
- **변수**: `KAKAO_REST_API_KEY="5217ea7ab053dec8ecb03d4551b5b0b8"`
- **용도**: 지도 검색 기능 (`/api/v1/places/search`)

### 2. 스크립트 (Python)
- **파일**: `scripts/.env`
- **변수**: `KAKAO_API_KEY="5217ea7ab053dec8ecb03d4551b5b0b8"`
- **용도**:
  - CSV 데이터 좌표 변환 (`add_coordinates.py`)
  - 원시 데이터 시드 (`reseed_from_raw.py`)

## 🔧 설정 동기화 방법

### 자동 동기화 스크립트
```bash
# API 키 변경 시 양쪽 .env 파일을 동시에 업데이트하는 스크립트
cat > sync-kakao-key.sh << 'EOF'
#!/bin/bash
if [ -z "$1" ]; then
  echo "사용법: ./sync-kakao-key.sh 새_API_키"
  exit 1
fi

NEW_KEY="$1"

# API 서버 .env 업데이트
sed -i "s/KAKAO_REST_API_KEY=.*/KAKAO_REST_API_KEY=\"$NEW_KEY\"/" api_server/.env

# 스크립트 .env 업데이트
sed -i "s/KAKAO_API_KEY=.*/KAKAO_API_KEY=\"$NEW_KEY\"/" scripts/.env

echo "✅ 카카오 API 키가 동기화되었습니다: $NEW_KEY"
EOF
chmod +x sync-kakao-key.sh
```

## 📊 사용 현황

### API 서버 검색 기능
- **테스트 완료**: ✅ "강남역" 검색 결과 15개 반환
- **응답 시간**: ~200ms
- **기능**: 실시간 장소 검색, 자동완성, 지도 이동

### 스크립트 지오코딩
- **테스트 준비**: scripts 디렉토리에서 `python3 add_coordinates.py test` 실행
- **기능**: CSV 주소 → 좌표 변환, 데이터베이스 시드

## 🚨 보안 주의사항

1. **Git 추적 방지**: `.env` 파일들이 `.gitignore`에 포함되어 있는지 확인
2. **키 노출 방지**: 로그나 에러 메시지에 API 키가 포함되지 않도록 주의
3. **키 교체**: 필요 시 위의 동기화 스크립트를 사용하여 일관성 유지

## ✅ 검증 완료

- [x] API 서버에서 카카오 검색 API 정상 작동
- [x] scripts/.env에 API 키 설정 완료
- [x] 양쪽 환경에서 동일한 API 키 사용 확인

이제 지도 검색 기능과 데이터 파이프라인이 동일한 카카오 API 키를 공유하여 일관성 있게 작동합니다! 🎯